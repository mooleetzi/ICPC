#include <bits/stdc++.h>
#include <windows.h>
using namespace std;
int main()
{
    while (1)
    {
        system("data");
        system("biaoda");
        system("test");
        if (system("fc biaoda.out test.out"))
            break;
    }
    return 0;
}