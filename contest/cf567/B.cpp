#include<iostream>
#include<algorithm>
#include<vector>
#include<string>
using namespace std;
int l;
string s;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin >> l;
    cin >> s;
    int mid = l >> 1;
    if (s[mid])
    return 0;
}